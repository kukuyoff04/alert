# ตำแหน่งไฟล์: .github/workflows/water_alert.yml
# ‼️ นี่คือเวอร์ชันอัปเดต ‼️

name: 🌊 Inburi Water Level Alert

on:
  schedule:
    # 1. ตั้งเวลาทำงาน: รันทุก 1 ชั่วโมง
    - cron: '0 * * * *'
  workflow_dispatch:
    # 2. เพิ่มปุ่มให้กดรันเองได้

jobs:
  scrape-and-notify:
    runs-on: ubuntu-latest
    
    # --- ⬇️ เพิ่มส่วนนี้ ⬇️ ---
    # 3. อนุญาตให้ Action นี้เขียนไฟล์กลับมาที่ Repo ได้
    permissions:
      contents: write
    # --- ----------------- ---
      
    steps:
      - name: 1. Check out repository
        uses: actions/checkout@v4

      - name: 2. Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 3. Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 4. Install Playwright browser
        run: playwright install --with-deps chromium

      - name: 5. Run scraper script
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
        run: python scrape_water.py
      
      # --- ⬇️ เพิ่มส่วนนี้ทั้งหมด ⬇️ ---
      - name: 6. Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: 7. Commit and push changes
        run: |
          # ตรวจสอบว่ามีไฟล์ last_level.txt ที่เปลี่ยนแปลงหรือไม่
          git add last_level.txt
          
          # ตรวจสอบสถานะ ถ้าไม่มีอะไรเปลี่ยนแปลง ให้ออก
          if git diff-index --quiet HEAD; then
            echo "No changes in water level, skipping commit."
          else
            # ถ้ามีการเปลี่ยนแปลง ให้ commit และ push
            git commit -m "Update last water level"
            git push
          fi
